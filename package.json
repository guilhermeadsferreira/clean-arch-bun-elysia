{
  "name": "clean-arch-bun-elysia",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "prisma:generate": "bunx prisma generate --schema=./src/external/prisma/schema.prisma",
    "prisma:push": "bunx prisma db push --schema=./src/external/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "5.3.1",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.3.1"
  },
  "module": "src/index.js"
}